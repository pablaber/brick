{
  "name": "@workout/strava-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc --watch --noEmit",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests yet for @workout/strava-worker\""
  },
  "dependencies": {
    "@workout/shared": "workspace:*"
  }
}
